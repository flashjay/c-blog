#ifndef __PANEL_USER_H__
#define __PANEL_USER_H__


bool panel_user_is_login(struct env_t *_SERVER);

void panel_user_login(struct env_t *_SERVER);

void panel_user_quit(struct env_t *_SERVER);

#endif

